# Qwen / Voxel Craft

An unchanged snapshot of `mc-html-home/index.html`, including its uncommitted working-tree improvements after `f598ceeab771066589079a79d14a6f7caf3b83fd`.

The author identifies the local model as `Qwen3.8-27B-UD-Q4_K_S.gguf`, running through pi.dev. They estimate approximately four full 128K contexts and several iterations. The agent initially lacked internet access; `pi install --local npm:pi-web-access` was added later. A registered local package entry was confirmed during capture.

Seven commits were present in the source repository. Messages explicitly record a handoff from context one and the end of context two. The approximate four-context account comes from the author rather than a reconstructed token log.

The game is preserved for comparison, with no repairs or interface additions made during the showcase task. Its handoff and memory notes are historical, and some TODOs were addressed by later uncommitted changes.

The publication plan uses `mc-html-home` as the Git repository base to retain the original iteration history. See the root README before migrating.
